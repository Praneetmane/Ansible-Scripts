# Cron job to git clone/pull a repo and then run locally
{{ schedule }} root ansible-pull -U {{ repo_url }} -C {{ branch_name }} -i inventory.txt -d {{ workdir }} -o packages.yml -t "cleanup" >>{{ logfile }} 2>&1
